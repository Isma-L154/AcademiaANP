@model IEnumerable<ANP_Academy.DAL.Models.Comentario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContenidoComentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaComentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContenidoComentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaComentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoUsuario.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdComentario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdComentario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdComentario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
